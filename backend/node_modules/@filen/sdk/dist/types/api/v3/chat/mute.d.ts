import type APIClient from "../../client";
export declare class ChatMute {
    private readonly apiClient;
    constructor({ apiClient }: {
        apiClient: APIClient;
    });
    fetch({ uuid, mute }: {
        uuid: string;
        mute: boolean;
    }): Promise<void>;
}
export default ChatMute;
