export class ChatMute {
    apiClient;
    constructor({ apiClient }) {
        this.apiClient = apiClient;
    }
    async fetch({ uuid, mute }) {
        await this.apiClient.request({
            method: "POST",
            endpoint: "/v3/chat/mute",
            data: {
                uuid,
                mute
            }
        });
    }
}
export default ChatMute;
//# sourceMappingURL=mute.js.map